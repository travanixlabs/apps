# Saved listing pages and the spreadsheet built from them: 9 MB of scraped HTML
# that the tools read and can re-read, not source. Kept on disk, out of history.
en.xchina.co/
# The same, for AV Jiali. These pages turned out not to name anybody at all --
# the cast lives on the site's model pages -- so tools/avjiali-cast.js fetches
# it instead and keeps the answer in avjiali-cast.json, which IS tracked.
avjiali.com/
# Undo manifests from tools/xchina-apply.js — local records of a local rename.
tools/manifests/
# Local settings: the folder you last opened, your card size, your volume.
config.json
# The mobile web app is its own repository with its own remote.
mobile/
# onnxruntime, pulled in for familiar faces. 280MB of native binaries for every
# platform; build.ps1 copies what it needs into the app. `npm install` restores it.
node_modules/
package-lock.json
